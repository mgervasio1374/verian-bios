{
  "meta": {
    "test_case_id": "TC-LA-037",
    "scenario_name": "lookback_window_applied_correctly",
    "description": "Events outside lookback window must not be counted. Only events within the window contribute to signals."
  },
  "input": {
    "events_in_window": 10,
    "events_outside_window": 100
  },
  "expected": {
    "denominator_uses_only_window_events": true,
    "pre_window_events_excluded": true
  }
}
